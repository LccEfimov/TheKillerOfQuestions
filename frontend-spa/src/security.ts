const te=new TextEncoder();
export async function deriveKey(pass:string,salt:Uint8Array){const m=await crypto.subtle.importKey('raw',te.encode(pass),{name:'PBKDF2'},false,['deriveKey']);return crypto.subtle.deriveKey({name:'PBKDF2',salt,iterations:120000,hash:'SHA-256'},m,{name:'AES-GCM',length:256},false,['encrypt']);}
export async function encryptPayload(payload:unknown,passphrase:string){const salt=crypto.getRandomValues(new Uint8Array(16));const iv=crypto.getRandomValues(new Uint8Array(12));const key=await deriveKey(passphrase,salt);const data=te.encode(JSON.stringify(payload));const ct=await crypto.subtle.encrypt({name:'AES-GCM',iv},key,data);return {version:'1.0',algorithm:'AES-256-GCM',kdf:'PBKDF2-SHA-256',salt:btoa(String.fromCharCode(...salt)),iv:btoa(String.fromCharCode(...iv)),ciphertext:btoa(String.fromCharCode(...new Uint8Array(ct))),metadata:{ts:new Date().toISOString(),payloadType:'analysis'}};}
export const lsbStegoEmbed=()=>({status:'experimental'});
