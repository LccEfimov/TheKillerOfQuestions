# Extension guide

- Add new features in `brain-service/src/analyze.js` and update weight matrix.
- Add new UI widgets in `frontend-spa/src` bound to API fields.
- Extend native summary in `native-guard/src/main.cpp` with new safe metrics.
