# Module contracts

`POST /analyze` input:
```json
{ "text": "...", "protectedMode": false }
```

Response includes:
`hero`, `stats`, `scenarioBars`, `riskRadar`, `comparison`, `evidence`, `timeline`, `pipeline`, `scene`, `conclusion`, `protection`, `native`.
